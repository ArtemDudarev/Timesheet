# Баги ручного тестирования

Формат находки — одна строка в таблице своей секции. Всё, что нужно: экран/шаг из
`MANUAL_TESTING.md` (если применимо), что ожидали, что получили. Severity и Status
проставляю я при разборе.

**Severity**: 🔴 блокер (нельзя пройти сценарий) · 🟠 баг (неверное поведение) · 🟡 мелочь (UI/косметика)
**Status**: `new` → `triaged` → `fixed` / `wontfix` (дубль П1–П3 бэклога) / `known` (уже в BACKLOG.md)

---

## Новые (не разобраны)

| # | Экран / шаг | Ожидалось | Получилось | Severity | Status |
|---|---|---|---|---|---|

## В работе / разобраны

| # | Экран / шаг | Ожидалось | Получилось | Severity | Status | Комментарий |
|---|---|---|---|---|---|---|

## Исправлено

| # | Экран / шаг | Что было | Фикс | Коммит/файл |
|---|---|---|---|---|
| 1 | Логин по временному паролю (после «Забыли пароль») | Экран форс-смены пароля сразу после ввода temp-пароля (П1 бэклога: must_change_password) | Фронт игнорировал флаг `must_change_password` из ответа логина | `screen-login.jsx`: `ForceChangePasswordModal`, показывается когда `data.must_change_password===true` |
| 2 | Управление → Сотрудники, бейдж на вкладке | Бейдж = число заявок, которые реально можно обработать (есть кнопка на карточке) | Висела «1» даже после смены пароля — считались и заявки без сопоставленного сотрудника (нерезолвимые бэком) | `screen-manager.jsx`: `pendingPwd` фильтруется по `resetFor`-матчингу; призрачная заявка `but-hed@mail.ru` отменена в БД |
| 3 | Клик по уведомлению «Заявка на сброс пароля» | Открывает Управление → вкладка «Сотрудники» (там кнопка выдачи пароля) | Открывало Управление, но всегда на вкладке «Проекты» | `app.jsx`: `notifTargetFor` теперь возвращает `{screen,tab}`; `screen-manager.jsx`: `ManagerScreen` принимает `navTab` и переключает вкладку эффектом (сработает даже если экран уже открыт) |
| 4 | Выдача временного пароля (повторно тому же сотруднику) | Каждая выдача — новый непохожий пароль | Пароль выглядел «одинаковым» — бэк генерировал случайный суффикс, но все начинались с захардкоженного `Tx1!`; проверено напрямую: 5 подряд выданных паролей были все разные, дело было в предсказуемом префиксе, а не в отсутствии рандома | `employee_service.py`: `_generate_temp_password()` без фиксированного префикса (2 загл + 4 строч + 3 цифры + 1 спецсимвол, перемешано `secrets.SystemRandom`) |
| 5 | Логин по email | Регистр email не важен (`ADMIN@demo.com` = `admin@demo.com`) | Сравнение `User.email == login_identity` было регистрозависимым | `employee_service.py`: `get_by_identity` → `func.lower(User.email) == login_identity.lower()`; тест `test_login_email_case_insensitive`, 39/39 |
| 6 | Смена пароля: новый = текущий | Ошибка «Новый пароль должен отличаться от текущего» | Уже так и работало — проверил вручную (422) и нашёл существующий тест `test_change_password_same_password`; фикс не потребовался | — |
| 7 | Управление → Сотрудники, заявка `MaNAger@demo.com` (регистр отличается от email сотрудника) | Флаг/кнопка «Временный пароль» появляются на карточке | Заявка видна в уведомлениях, но кнопка/бейдж не появлялись — заявка «не находила» сотрудника | `screen-manager.jsx`+`employee-card.jsx`: общая `resetRequestMatches()` без учёта регистра; заодно `password_reset_request_service.py`: дедуп заявок теперь по резолвнутому `user_id`, а не по сырой строке (иначе можно было наспамить дублей меняя регистр) |
| 8 | Форс-смена пароля после выдачи временного (вход по временному → ввод пароля, который был ДО сброса) | Ошибка «Новый пароль не должен совпадать с предыдущим паролем» | Ошибки не было — старый хеш нигде не хранился, сравнивать было не с чем | `User.previous_hashed_password` (новое поле, ручной `ALTER TABLE` — в проекте нет Alembic) заполняется в `change_password()`/`reset_password()` перед перезаписью хеша, проверяется при следующей смене |
| 9 | Профиль → карточка «Утилизация» | В начале месяца утилизация при 100%-й фактической загрузке близка к 100% | Утилизация считалась от нормы ВСЕГО месяца независимо от текущего дня — 1-го числа при полной загрузке показывало ~5-10% | `summary_service.py`: для текущего месяца норма для утилизации — «на сегодня» (`_calc_norm(upto=date.today())`), для прошлых месяцев — полная, как раньше |
| 10 | Профиль → карточка «Активных проектов» | 1 из 2 (только проект в статусе «Активный»; «Мобильное приложение» в статусе «Планируется» не считается активным, даже если сотрудник к нему уже привлечён) | Показывало «0 из 2» — `management_service` слал в Kafka `assignment_status.name` («Привлечён») вместо `.code`, сравнение никогда не совпадало | Уточнил у пользователя определение «активного проекта» — оказалось, это статус ПРОЕКТА, а не назначения. `events.py`: у назначения `.code`, у проекта добавлено отдельное поле `status_code` (само `status`-имя нельзя было трогать — его текстом показывает `reporting_service`). `timesheet_service.Project.status_code` + `_project_counts` считает уникальные проекты с `status_code=="ACTIVE"`. Данные в БД смигрированы вручную |
| 11 | Профиль → донат «Загрузка» vs карточка «Утилизация» | Понятно, что каждое число значит и почему они разные | Оба считались из одного и того же `logged_hours`, просто с разным знаменателем (донат — от полной месячной нормы, карточка — от нормы «на сегодня») — выглядели как дубли с расхождением | По уточнению у пользователя развели по смыслу: «Загрузка» = текущий месяц в темпе (как было), «Утилизация» = отдельная метрика за прошлый ЗАВЕРШЁННЫЙ месяц с трендом к позапрошлому. Новое поле `prev_month_utilization_pct`, `utilization_delta` теперь «прошлый минус позапрошлый» |
| 12 | Табель, ввод часов на проект/задачу за день | Нельзя списать больше 24ч суммарно за календарный день, ни при каких обстоятельствах | Реальный кейс: на один день набралось 58ч (8 WORK + 2+24+24 OVERTIME) — фронт капал только ОДНУ ячейку до 24, бэк при переполнении нормы дня автоматом создаёт OVERTIME без всякого верхнего предела | Фронт: `commitCell` теперь сверяет СУММУ за день по всем строкам (`colTotal`), не одну ячейку. Бэк (реальная дыра — фронт обходится прямым API): `time_entry_service.py`, `MAX_DAILY_HOURS=24`, проверка в `create()`/`update()` по сумме ВСЕХ записей дня. Тест `test_time_entry_daily_cap.py`, смоук подтверждён (400 при попытке превысить) |
